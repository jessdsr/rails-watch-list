<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <!-- par convention, simple_form_for retire le _id-->
  <%= f.association :movie %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
